[ENV]
scheme=https
name=bas-esup
domain=${name}.pc-scol.fr

[AUTH]
username=
password=
service=${ENV:scheme}://authn-app.${ENV:domain}/cas/v1/tickets

[REF]
version=v1
base_url=${ENV:scheme}://ref.${ENV:domain}/api/${version}/ref

[MOF]
version=v1
base_url=${ENV:scheme}://mof.${ENV:domain}/api/${version}/mof

[INS]
version=v5
base_url=${ENV:scheme}://ins.${ENV:domain}/api/${version}/ins

